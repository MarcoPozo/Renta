PORT=3000
NODE_ENV=development

# MySQL (XAMPP) — si root no tiene contraseña, dejar DB_PASS vacío
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=renta_db

# JWT (mínimo 32 caracteres)
JWT_SECRET=
JWT_EXPIRES_IN=7d

# CORS (vacío = abierto, solo para desarrollo)
CORS_ORIGIN=
